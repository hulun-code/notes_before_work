# Pytorch 安装 review
1.确定安装的是GPU还是CPU
2.安装Anaconda，这是控制python版本和python各个包的部分。安装时要把环境变量加入到系统当中。

preview
似乎没有展示conda install pytorch的界面，需要到pytorch界面中查看版本。

# Douyin tutorial
mmcv

尝试修改文件夹名称，里面有space，对一些package不友好
修改为programfile，重新安装。pacakage ache比较花实践
安装好了
根据CSDN，创建环境new，激活环境，并且安装numpy试试

agent
工具调用，react，plan and execute，reflection，RAG and Memory，Multi-agent（协作成本），graph/workflow---demo走向生产-按流程
逐层增强

