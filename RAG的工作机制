# Brief review on the video
检索增强生成，检索和生成两个步骤。首先是分片，然后根据分片生成的向量去与问答前的问题匹配，接着匹配出来的片段丢给大模型，最后得到输出。

索引
向量  数字序列，维度是个数，低维度可以画出来。当然是1，2，3维。一般比较大，几百到几千，信息丰富
Emebdding。向量接近。含义相近的文本，通过embedding得到相近的向量。有具体的emedding模型评测王章
向量数据库，存储和查询相近的向量。最终需要的是原始文本。包含两个
处理完所有文本

分片和索引是提问前

召回
最为相关的10个用户片段内容，可以灵活。相似的片段。
问题与向量数据库去计算相似度。similarity。余弦相似度，欧氏距离，点积。
夹角，距离，点积。
向量相似度

重排
与召回一样，10份里面再挑3份。召回加重排，文本相似度计算逻辑不同
相似度计算，成本低，耗时短，准确率低
cross-encoder的成本高，耗时长，准确率高一些

生成
问题，问题相关三个片段，发给大模型去生成

RAG整体流程
准备部分（提问前） 分片和索引
提问后  召回、重排和生成

资料，分片，embedding，向量，向量数据库---知识库构建
问题，embedding，向量，召回10个，crosscoder重排，一起发给大模型产出最终答案

